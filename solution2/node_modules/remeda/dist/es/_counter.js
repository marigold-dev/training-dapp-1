import { map } from './map';
export var createCounter = function () {
    var count = jest.fn();
    return {
        count: count,
        fn: function () {
            return map(function (x) {
                count();
                return x;
            });
        },
    };
};
