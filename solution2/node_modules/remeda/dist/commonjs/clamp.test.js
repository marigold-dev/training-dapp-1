"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var clamp_1 = require("./clamp");
it('min value', function () {
    expect(clamp_1.clamp(10, { min: 20 })).toEqual(20);
});
it('max value', function () {
    expect(clamp_1.clamp(10, { max: 5 })).toEqual(5);
});
it('ok value', function () {
    expect(clamp_1.clamp(10, { max: 20, min: 5 })).toEqual(10);
});
