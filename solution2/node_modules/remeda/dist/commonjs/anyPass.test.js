"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var anyPass_1 = require("./anyPass");
var fns = [function (x) { return x === 3; }, function (x) { return x === 4; }];
describe('data first', function () {
    test('anyPass', function () {
        expect(anyPass_1.anyPass(3, fns)).toEqual(true);
        expect(anyPass_1.anyPass(4, fns)).toEqual(true);
        expect(anyPass_1.anyPass(1, fns)).toEqual(false);
    });
});
describe('data last', function () {
    test('anyPass', function () {
        expect(anyPass_1.anyPass(fns)(3)).toEqual(true);
        expect(anyPass_1.anyPass(fns)(4)).toEqual(true);
        expect(anyPass_1.anyPass(fns)(1)).toEqual(false);
    });
});
