"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var zipWith_1 = require("./zipWith");
var pred = function (a, b) { return a + b; };
var first = ['1', '2', '3'];
var second = ['a', 'b', 'c'];
var shorterFirst = ['1', '2'];
var shorterSecond = ['a', 'b'];
describe('data first', function () {
    test('should zip with predicate', function () {
        expect(zipWith_1.zipWith(first, second, pred)).toEqual(['1a', '2b', '3c']);
    });
    test('should truncate to shorter second', function () {
        expect(zipWith_1.zipWith(first, shorterSecond, pred)).toEqual(['1a', '2b']);
    });
    test('should truncate to shorter first', function () {
        expect(zipWith_1.zipWith(shorterFirst, second, pred)).toEqual(['1a', '2b']);
    });
});
describe('data first typings', function () {
    test('infers typing from predicate', function () {
        var actual = zipWith_1.zipWith(first, second, pred);
        var result = true;
        expect(result).toBe(true);
    });
});
describe('data second', function () {
    test('should zip with predicate', function () {
        expect(zipWith_1.zipWith(pred)(first, second)).toEqual(['1a', '2b', '3c']);
    });
    test('should truncate to shorter second', function () {
        expect(zipWith_1.zipWith(pred)(first, shorterSecond)).toEqual(['1a', '2b']);
    });
    test('should truncate to shorter first', function () {
        expect(zipWith_1.zipWith(pred)(shorterFirst, second)).toEqual(['1a', '2b']);
    });
});
describe('data second typings', function () {
    test('infers typing from predicate', function () {
        var actual = zipWith_1.zipWith(pred)(first, second);
        var result = true;
        expect(result).toBe(true);
    });
});
describe('data second with initial arg', function () {
    test('should zip with predicate', function () {
        expect(zipWith_1.zipWith(pred, second)(first)).toEqual(['1a', '2b', '3c']);
    });
    test('should truncate to shorter second', function () {
        expect(zipWith_1.zipWith(pred, shorterSecond)(first)).toEqual(['1a', '2b']);
    });
    test('should truncate to shorter first', function () {
        expect(zipWith_1.zipWith(pred, second)(shorterFirst)).toEqual(['1a', '2b']);
    });
});
describe('data second with initial arg typings', function () {
    test('infers typing from predicate', function () {
        var actual = zipWith_1.zipWith(pred, second)(first);
        var result = true;
        expect(result).toBe(true);
    });
});
