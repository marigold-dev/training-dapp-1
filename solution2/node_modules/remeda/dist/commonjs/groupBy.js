"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.groupBy = void 0;
var purry_1 = require("./purry");
/**
 * Splits a collection into sets, grouped by the result of running each value through `fn`.
 * @param fn the grouping function
 * @signature
 *    R.groupBy(fn)(array)
 * @example
 *    R.pipe(['one', 'two', 'three'], R.groupBy(x => x.length)) // => {3: ['one', 'two'], 5: ['three']}
 * @data_last
 * @indexed
 * @category Array
 */
function groupBy() {
    return purry_1.purry(_groupBy(false), arguments);
}
exports.groupBy = groupBy;
var _groupBy = function (indexed) { return function (array, fn) {
    var ret = {};
    array.forEach(function (item, index) {
        var value = indexed ? fn(item, index, array) : fn(item);
        var key = String(value);
        if (!ret[key]) {
            ret[key] = [];
        }
        ret[key].push(item);
    });
    return ret;
}; };
(function (groupBy) {
    function indexed() {
        return purry_1.purry(_groupBy(true), arguments);
    }
    groupBy.indexed = indexed;
})(groupBy = exports.groupBy || (exports.groupBy = {}));
